object KeyStores {
    object Debug {
        const val debugStoreFile = "../socialite-debug-key.jks"
        const val debugStorePassword = "DebugKey"
        const val debugStoreAlias = "Socialite-Debug-Key"
    }

    object Local {
        const val debugStoreFile = "../socialite-debug-key.jks"
        const val debugStorePassword = "DebugKey"
        const val debugStoreAlias = "Socialite-Debug-Key"
    }
}
